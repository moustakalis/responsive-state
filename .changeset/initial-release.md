---
'responsive-state': minor
---

Initial release: `matchMedia`-driven, dependency-free breakpoint store with typed breakpoint names, `is`/`up`/`down`/`between`/`pick` helpers, feature queries, SSR snapshots, optional viewport tracking and `data-breakpoint` mirroring.
